# Copy this file to .env at the PROJECT ROOT and paste your real key there.
# The root .env is gitignored — this .example file is committed, so it must
# never contain a real key.
GEMINI_API_KEY=your-api-key-here
